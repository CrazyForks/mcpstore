# MCPStore Rust Core Session 与共享后端推进计划

## 目标

把 session 从 Python SDK 的内存兼容层升级为 Rust core 的一等业务对象。Python、Rust agent、API、MCP server 后续都应消费同一套 session 语义；共享后端作为跨进程事实来源；Python 只保留 SDK 体验层。

当前不能再把 session 理解为 Python 私有对象。成熟设计应共享可序列化业务状态，让每个进程根据 session 状态自行 hydrate 服务连接。连接池、MCP client handle、transport runtime 仍然是进程内运行态，不能跨进程共享，也不应伪装成可共享对象。

## 当前判断

- 核心业务主链路已经大体迁移到 Rust：store/agent scope、服务管理、工具调用、resources、prompts、adapter 入口基本存在。
- session 已从 Python facade 内存状态迁到 Rust core。Python `_sessions` 现在只能作为 wrapper cache；真实业务状态由 Rust session entity/relation/status/state/event 管理。
- session 跨进程共享技术可行。现有 cache 架构已有 entity/relation/state/event 四层，适合新增 session 领域模型。
- 共享后端与 `only_db` 方向正确。Memory backend 只保证单个 Rust store 实例内共享；真正跨 store 实例/跨进程共享需要 Redis/openkeyv Redis。Redis CAS 已实现，Redis 双 store 实例 E2E 测试已补；Python facade 与 PyO3 Rust core 跨进程共享 session 已用真实 Redis 验证。
- session-scoped state/cache 已作为正式 Rust core `session_state` state record 落地。它只保存 JSON 可序列化业务状态，按 session_key 聚合 values，使用 version/CAS 写入，并参与 snapshot/export/import/switch/inspect；Python `clear_cache()` 现在清理该 session 的正式 state，不再是 NotImplemented。
- Rust core 已有 `switch_cache_storage` 的运行时迁移能力，并且 session 集合能随 snapshot 迁移；Python `switch_cache()` 已改为调用 Rust runtime migration，并在迁移后清理 Python wrapper cache。
- API 已增加 session create/get/find/list/status/close/extend/bind/unbind/list_services/list_tools/call_tool 路由。成熟入口优先使用 query/body 传 `session_key`，旧 path 形式保留兼容；缺失 session 的 get/status 返回 404，不再成功返回 null。
- API 已增加 `session_state` 管理路由：set/get value/list/delete/clear 都直接调用 Rust core `session_state`，并覆盖 closed session 写入拒绝；path 形式的 clear 不要求无意义 body。
- MCP server 已支持显式业务 session：启动参数 `--session-key` 固定整个 server 的工具/resources/prompts/call 视图；单次工具调用可通过 `_meta._mcpstore_session_key` 或 arguments `_mcpstore_session_key` 覆盖，且该控制字段不会转发给真实工具。rmcp transport session 仍与 MCPStore 业务 session 分离。
- MCP server 已增加默认关闭的 `session_state` 管理工具暴露能力；显式开启后提供 list/get/set/delete/clear 五个 `mcpstore_session_state_*` 工具，直接调用 Rust core，返回 structured content，并在管理工具名与下游工具冲突时拒绝启动。
- 旧 Python tool transformation 的可共享部分已升级为 Rust core `tool_transforms` registry：支持工具展示名、描述覆盖、参数重命名、隐藏参数默认值、参数描述、tags/enabled；scoped/session/agent 工具列表和调用解析都走同一套 Rust core 规则。旧 Python callable hooks 不进入共享核心，Python 兼容 wrapper 会明确抛 `NotImplementedError`。
- API 已增加 `/for_store/tool_transforms/*` 管理路由，直接调用 Rust core registry；MCP server 已增加默认关闭的 `tool_transform` 管理工具，显式开启后提供 list/get/set/delete，且 `list_tools` 和新工具调用会按 Rust core 当前规则动态重建绑定，不要求重启 server 才看到新规则。
- 旧 Python OpenAPI import 的可共享部分已升级为 Rust core `openapi_imports` state record；OpenAPI 导入现在会注册 `transport=openapi` 虚拟服务，写入标准 service/tool/status 投影，并由 Rust core 执行 HTTP tool/resource/resource_template。PyO3/Python facade、API、MCP server 和 Rust agent 共用同一套 store 能力；securitySchemes、常见 request body、参数序列化、multipart 文件契约、二进制响应、JSON/YAML 外部 `$ref`、HTTP(S)/文件系统 `$ref` bundle、基础 bundle 输出 API、基础 bundle artifact 输出、artifact document hash/size metadata、API/MCP bundle 与 bundle artifact 管理入口、HTTP(S) external `$ref` 共享文档缓存已有成熟基础版；OAuth/OpenID flow、secret store、streaming、大文件 artifact store、source map/完整诊断级 bundle artifact、root spec 持久缓存、file `$ref` 持久缓存、ETag/Last-Modified 条件请求仍作为后续能力，不伪装已完成。
- MCP server stdio 与 streamable-http transport E2E 已打开为默认测试：真实启动 Rust `mcp-server`，连接下游 stdio fixture，并验证 tools/resources/prompts/call 由同一套 Rust core 聚合能力提供。
- Rust core 服务连接已按 `health_check.startup_timeout` 增加启动超时；下游 stdio service 卡住时会进入明确错误和 circuit-open 状态，不再无限等待。

## 设计原则

1. Rust core 是 session 事实来源。
2. Python 只做体验层和对象包装，不保存真实业务状态。
3. 跨进程共享 session metadata、relation、status、policy，不共享连接句柄。
4. session 语义必须同时支持 Python agent 和 Rust agent。
5. API/MCP server 后续接入同一套 session，不另造语义。
6. 传输层 session 与业务 session 分离。rmcp 的 `LocalSessionManager` 不是 MCPStore 业务 session。
7. session 数据必须参与后端迁移、导出导入和审计。
8. 不做临时 Python dict 兜底，不用不可验证的兼容层掩盖 Rust core 能力缺口。

## 目标数据模型

### Entity

新增 `sessions` entity。

建议结构：

```rust
pub struct SessionEntity {
    pub session_key: String,
    pub session_id: String,
    pub scope: SessionScope,
    pub agent_id: Option<String>,
    pub created_at: i64,
    pub updated_at: i64,
    pub last_active: i64,
    pub lease_seconds: Option<i64>,
    pub expires_at: Option<i64>,
    pub version: u64,
    pub metadata: serde_json::Value,
}
```

### Relation

新增 session relation：

- `session_services`: session -> bound services
- `session_tools`: session -> explicit tool visibility / override

建议结构：

```rust
pub struct SessionServiceRelation {
    pub session_key: String,
    pub services: Vec<SessionServiceItem>,
    pub updated_at: i64,
    pub version: u64,
}

pub struct SessionToolVisibility {
    pub session_key: String,
    pub tools: Vec<SessionToolItem>,
    pub mode: ToolVisibilityMode,
    pub updated_at: i64,
    pub version: u64,
}
```

### State

新增 `session_status` 与 `session_state` state。

建议状态：

- `active`
- `closed`
- `expired`

`session_state` 用于 session-scoped 业务状态/cache，不保存连接句柄或 Python 私有对象：

```rust
pub struct SessionStateData {
    pub session_key: String,
    pub values: serde_json::Map<String, serde_json::Value>,
    pub updated_at: i64,
    pub version: u64,
}
```

### Event

新增 `session_events` event，至少记录：

- create
- bind_service
- unbind_service
- set_tool_visibility
- extend
- close
- expire
- call_denied

## 关键语义决策

| 决策点 | 采用方案 | 说明 |
| --- | --- | --- |
| Session 归属 | Rust core 管理 | Python/Rust/API/MCP 后续统一消费 |
| 作用域 | 同时支持 `store` 和 `agent` | 兼容旧 Python，也支持 agent 隔离 |
| 唯一键 | `{scope}:{agent_id_or_global}:{session_id}` | 不同 agent 可复用同名 session |
| 默认过期 | 默认不过期 | 避免旧业务长任务被意外清理 |
| TTL | 模型内置 `expires_at`，不依赖 Redis TTL | 保持 memory/redis/openkeyv 行为一致 |
| 关闭 | 软关闭，保留审计 | closed/expired session 拒绝工具调用 |
| 未绑定服务 | 继承当前 scope 可用服务 | 兼容旧 Python 行为 |
| 已绑定服务 | 只能访问绑定服务 | session 才有边界意义 |
| 工具可见性 | session visibility 与 agent visibility 取交集 | agent 是长期权限，session 是任务级权限 |
| 一致性 | optimistic concurrency + version/updated_at | 不做临时 last-write-wins |
| Redis 并发 | 后端支持 CAS 或等价 compare-and-put | shared Redis 下避免覆盖更新 |
| session cache | 正式 `session_state` state record | Rust/Python/Rust agent 共享 JSON 业务状态，不共享运行态句柄 |
| tool transformation | 正式 `tool_transforms` state record | 只共享声明式、JSON 可序列化规则；不共享 Python callable hook |
| 后端迁移 | session 数据必须随 snapshot 迁移 | session 是核心业务状态 |

## Rust 端改造清单

### P0: Core model 与 cache 集合

- [x] 在 `rust/crates/mcpstore/src/cache/models.rs` 增加 session 相关模型。
- [x] 在 cache collection 校验中加入 `sessions` entity。
- [x] 在 cache inspect 中加入 `sessions`、`session_services`、`session_tools`、`session_status`、`session_events`。
- [x] 确认 `CacheSnapshot` 自动覆盖新增集合，必要时补测试。

### P0: Session domain API

- [x] 新增 Rust core session 模块，例如 `rust/crates/mcpstore/src/session/` 或 `rust/crates/mcpstore/src/store/session.rs`。
- [x] 实现 `create_session(scope, agent_id, session_id, options)`。
- [x] 实现 `get_session(session_key)` / `find_session(scope, agent_id, session_id)`。
- [x] 实现 `list_sessions(scope, agent_id)`。
- [x] 实现 `close_session(session_key)`。
- [x] 实现 `extend_session(session_key, seconds)`。
- [x] 实现 `bind_service_to_session(session_key, service_name)`。
- [x] 实现 `unbind_service_from_session(session_key, service_name)`。
- [x] 实现 `list_session_services(session_key)`。
- [x] 实现 `set_session_tool_visibility(session_key, service_name, tools)`。
- [x] 实现 `list_session_tools(session_key)`。
- [x] 实现 `get/list/set/delete/clear_session_state`，以及 set/delete 的显式 retry helper。

### P0: Session-aware resolver 与调用

- [x] 新增 `call_tool_in_session(session_key, tool_name, args)`。
- [x] 新增 session-aware `list_tools` 路径。
- [x] 新增 session-aware resources/prompts 路径。
- [x] closed/expired session 必须拒绝调用。
- [x] 已绑定服务时只允许访问绑定服务内的工具。
- [x] 未绑定服务时继承当前 scope 的默认服务集合。

### P1: 并发一致性

- [x] 给 session entity/relation/status 增加 version 检查。
- [x] 在 cache trait 增加 compare-and-put 或同等 CAS 能力。
- [x] memory backend 使用进程内锁保证 CAS。
- [x] Redis backend 使用 Lua compare-and-put 保证 CAS。
- [x] CAS 冲突返回明确错误，调用方可重试。
- [x] Rust core 增加显式 `SessionRetryPolicy` 和 `*_with_retry` 写操作 helper；默认写入继续保持明确 CAS 冲突语义，不在底层静默重试或覆盖。
- [x] Redis CAS 真实跨 store 实例 E2E 已在临时 Redis 环境验证通过：`cargo test -p mcpstore redis_backend -- --nocapture`。

### P1: 后端迁移与导出

- [x] `switch_cache_storage` 必须迁移 session entity/relation/state/event。
- [x] `cache_inspect` 能看到 session 集合。
- [x] `export_to_json(include_sessions=True)` 导出 session entity/relation/status/state/event，不导出连接态。
- [x] session import 导入 session metadata/relation/status/state/event，不导入连接态；导入前做类型校验和冲突预检，幂等重放允许，差异冲突不覆盖。

### P1: API / MCP server / Rust agent

- [x] Rust crate 暴露 `SessionContext` / `SessionBuilder` 作为 Rust agent 入口。
- [x] API 增加 `/sessions` 系列接口：create/get/list/status/bind/unbind/close/extend/list_tools/call_tool。
- [x] API 支持 body/query 形式传业务 `session_key`，避免把带冒号的 session key 强绑定到 URL path 编码。
- [x] MCP server 支持业务 session id 输入，但不混同 rmcp transport session。
- [x] Rust agent 可直接通过 Rust crate 使用 session，不必绕 API。
- [x] API 增加 `/for_store/tool_transforms/*` 管理入口，Rust agent 可通过 HTTP 管理同一套 Rust core tool transform registry。
- [x] MCP server 增加默认关闭的 `mcpstore_tool_transform_*` 管理工具，显式开启后 Rust/MCP agent 可管理同一套 Rust core tool transform registry。

## Python 端改造清单

### P0: Facade 改造

- [x] `RustSession` 改成 Rust core session wrapper。
- [x] `create_session()` 调 Rust core 创建或获取 session。
- [x] `find_session()` / `list_sessions()` 从 Rust core 读取。
- [x] `bind_service()` / `close_session()` / `extend_session()` 调 Rust core。
- [x] `list_tools()` / `use_tool()` 走 session-aware Rust 方法。
- [x] `session_auto()` 只保存当前 Python context 的 active session id。
- [x] `_sessions` 只允许作为 wrapper cache，不再保存真实业务状态。

### P0: 兼容旧 Python 体验

- [x] 保留 `with_session()` 同步上下文管理器。
- [x] 保留 `with_session_async()` 异步上下文管理器。
- [x] 保留未绑定服务时看到 scope 全部服务的行为。
- [x] `extend_session()` 不再抛 `NotImplementedError`。
- [x] `clear_cache()` 清理 Rust core 正式 `session_state`，不清全局 cache，也不使用 Python 私有 dict。
- [x] 旧参数映射：`default_timeout -> lease_seconds`，`auto_cleanup -> session metadata policy flag`，`session_prefix -> Python auto session naming helper`。不在 Python 端实现伪后台清理。

### P1: Python 公共 API 兼容

- [x] 补齐旧版顶层导出：`ServiceInfo`、`ToolInfo`、`APIResponse`、`ResponseBuilder`、`ErrorCode`、异常类等。
- [x] 恢复旧版 `mcpstore.core.context` 公开入口，并补齐顶层 `Session` / `SessionContext` / `MCPStoreContext` 导出；这些对象都是 `RustSession` / `RustStoreContext` alias，不恢复旧 Python session 事实源。
- [x] 恢复旧版 context 子模块的可成立兼容入口：`base_context`、`session`、`service_proxy`、`tool_proxy`、`cache_proxy`、`types`、`agent_service_mapper`、`service_management.UpdateServiceAuthHelper`。proxy/session/cache/context 都指向 Rust-backed facade；mapper/result/helper 是无状态兼容工具。
- [x] 恢复旧版 `context.resources_prompts.ResourcesPromptsMixin` 导入和 resources/prompts 方法名，方法委托 Rust-backed context；`list_changed_tools` 已改为调用 Rust core 的按需工具刷新+diff 原子能力，不恢复旧 Python 进程内 monitor。
- [x] 恢复旧版 `context.store_proxy.StoreProxy` 与 `context.agent_proxy.AgentProxy` 导入和构造入口；它们只是 Rust-backed context 委托包装，不恢复旧 Python registry/orchestrator 代理逻辑。
- [x] 恢复旧版 context operations mixin 模块路径：`service_operations`、`tool_operations`、`session_management`、`agent_statistics`、`advanced_features`、`async_safe_service_management`、`tool_proxy_annotations`。可委托的服务/工具/session/统计行为都落到 Rust-backed context；OpenAPI import 已改为 Rust-backed 共享导入/分析；旧 Python service manager factory 明确不伪兼容。
- [x] 明确哪些旧模型是兼容 wrapper，哪些已被 Rust-backed model 替代：这些模型保留 Python SDK surface 兼容，store/session 真实业务行为仍由 Rust/PyO3 core 提供。
- [x] 不恢复旧 `SessionManagementMixin` 的 Python 状态逻辑；旧 tool transformation manager 已恢复为 Rust-backed 兼容 wrapper，只负责把声明式规则写入 Rust core `tool_transforms`，不恢复 Python 私有 transform 事实源。
- [x] 旧 Python callable hooks（`validation_fn`、`transform_fn`、pre/post execution hook）明确不进入共享核心；Python compatibility wrapper 对这些入口抛 `NotImplementedError`，避免伪装成跨进程/Rust agent 可用能力。

### P1: Python 后端迁移接入

- [x] PyO3 暴露 `switch_cache_storage(backend, redis_url, namespace)`。
- [x] Python `switch_cache()` 改为调用 Rust core runtime migration，而不是重建 store。
- [x] 迁移后 Python wrapper cache 会失效，避免继续持有旧后端语义下的 session wrapper。

## 测试与验收

### Rust tests

- [x] session create/get/list/close/extend。
- [x] store scope session 与 agent scope session 隔离。
- [x] 不同 agent 可使用同名 session。
- [x] bind service 后 list_tools 只返回绑定服务工具。
- [x] 未 bind service 时继承当前 scope 服务集合。
- [x] closed/expired session 拒绝工具调用。
- [x] session visibility 与 agent visibility 取交集。
- [x] switch_cache_storage 迁移 session 数据。
- [x] CAS 冲突测试。
- [x] session export 只包含可序列化业务状态。
- [x] `session_state` 支持 JSON 可序列化读写、delete、clear、version/CAS 与 closed session 写入拒绝。

### PyO3 tests

- [x] PyO3 暴露 session create/get/list/bind/close/extend/call_tool。
- [x] PyO3 暴露 session extend/bind/unbind 的显式 retry helper。
- [x] PyO3 暴露 Rust runtime `switch_cache_storage`。
- [x] PyO3 暴露 Rust core `export_sessions_snapshot()`。
- [x] PyO3 暴露 Rust core `import_sessions_snapshot()`。
- [x] PyO3 暴露 Rust core session state：get/list/set/delete/clear 与显式 retry helper。
- [x] PyO3 返回 dict/list 结构与 Python facade 期望一致。
- [x] 不再依赖 JSON string bridge。

### Python tests

- [x] 旧 `with_session()` 用法继续可用。
- [x] `session_auto()` 与 `session_manual()` 继续可用。
- [x] `extend_session()` 可用。
- [x] `clear_cache()` 清理 Rust core session-scoped state。
- [x] Python facade 暴露 `set_state()` / `set_state_with_retry()` / `get_state()` / `list_state()` / `state_values()` / `delete_state()` / `delete_state_with_retry()`。
- [x] Python facade 与 PyO3 Rust core 通过真实 Redis shared backend 跨进程看到同一 session。
- [x] 两个 Python backend wrapper 包同一个 Rust store 时看到同一 session state。
- [x] Python active session id 不污染 Rust core session 状态。
- [x] Python `switch_cache()` 调用 Rust runtime migration 并清理 wrapper cache。
- [x] Python `export_to_json(include_sessions=True)` 输出 Rust core session 快照。
- [x] Python `import_from_json()` 能恢复 Rust core session 快照。
- [x] Python facade 暴露 `extend_session_with_retry()` / `bind_service_with_retry()` / `unbind_service_with_retry()`，调用 Rust core retry helper。
- [x] Python 顶层和 `mcpstore.core.context` 兼容导出已验证：`Session` / `SessionContext` / `MCPStoreContext` / proxy/cache/resources_prompts/store_proxy/agent_proxy/operations mixin 类型都指向或委托 Rust-backed facade，旧 context 子模块可导入，session state 写读和 resources/prompts/服务/工具/session/统计查询仍调用 Rust core backend。

### API / MCP tests

- [x] API session 路由编译通过，`cargo test -p mcpstore-cli --test api_flow` 可编译现有 flow。
- [x] MCP server 业务 session 控制字段解析与清洗有单测覆盖。
- [x] API session 路由已有进程内真实 HTTP E2E：DB/shared cache 事实源 hydrate service/tool，HTTP create/bind/list_tools/close/closed-reject 覆盖同一套 Rust core session 语义；外部 CLI `api_flow` 仍因 stdio transport 环境差异为 ignored。
- [x] API `session_state` 路由已有进程内真实 HTTP E2E：body/query/path 三类入口覆盖 set/get value/list/delete/clear，closed session 写入返回 `SESSION_NOT_ACTIVE`。
- [x] MCP server 已有稳定 handler 集成测试：DB/shared cache 事实源 hydrate service/tool，server 构造按 Rust core session 过滤工具绑定，closed session 拒绝启动 active server view。
- [x] MCP server `session_state` 管理工具已有单测：默认不暴露，显式开启后出现 list/get/set/delete/clear 五个工具，调用落到同一 Rust core session_state。
- [x] API `tool_transforms` 管理路由已有进程内真实 HTTP E2E：set/get/list/delete 直接调用 Rust core，scoped tool list 立即体现展示名、描述和参数 schema 变换。
- [x] MCP server `tool_transform` 管理工具已有 handler 集成测试：默认不暴露，显式开启后出现 list/get/set/delete 四个工具，调用落到同一 Rust core registry；同一 server 的 `list_tools`/工具调用绑定会动态读取 Rust core 当前 transform 规则。
- [x] MCP server streamable-http/stdio transport E2E 已打开为默认测试：store stdio、agent-scope stdio、store streamable-http 三条 flow 均通过，并有 20s 总超时避免回归时挂死。

### Redis E2E

- [x] Python 创建 session，PyO3 Rust core 跨进程读取并更新 session lease。
- [x] PyO3 Rust core 创建 session，Python facade 跨进程读取 session。
- [x] Redis shared backend 切换后 session 数据仍在，并可被另一个进程读取。
- [x] 多个进程并发 bind/extend 压测通过；各进程自行 hydrate service，Redis 共享 session 业务状态，遇到 CAS/version 冲突可由调用方有限重试后收敛。
- [x] `session_state` Redis E2E 已在临时 Redis 环境验证通过：Rust 双 store 实例 set/get/delete/clear，Python facade 与 PyO3 Rust core 跨进程 set/get/delete/clear，switch_cache 后另一个进程读取 state。

## 阶段计划

### Phase 1: Rust core session 基础模型

目标：Rust core 能创建、读取、关闭、续期、绑定服务，并通过 cache 共享 session 元数据。

验收：Rust 单测通过，`cache_inspect` 能看到 session 集合。

### Phase 2: Session-aware 工具解析与调用

目标：Rust core 能按 session 限制 list_tools/call_tool/resources/prompts。

验收：绑定服务、未绑定服务、closed/expired、agent visibility 交集都有测试。

### Phase 3: PyO3 与 Python facade 对齐

目标：Python `with_session()` 体验不变，但底层状态来自 Rust core。

验收：Python session 测试不再依赖 `_sessions` 作为事实来源。

### Phase 4: 后端迁移、API、Redis E2E

目标：session 能随 backend 迁移，跨进程共享可验证，API/MCP/Rust agent 有统一入口。

验收：Redis E2E 证明 Python/Rust 双向创建和读取 session。

## 明确不做的临时方案

- 不把 Python `_sessions` 继续扩展成隐性核心。
- 不用 Python dict 模拟跨进程共享。
- 不把 rmcp `LocalSessionManager` 当 MCPStore 业务 session。
- 不共享连接池、client handle 或 transport runtime。
- 不把 session-scoped cache 做成 Python 私有缓存；正式能力必须走 Rust core `session_state`。
- 不宣称 Redis 共享完成，除非 Redis E2E 通过。

## 当前下一步

1. 按真实业务入口决定是否默认启用 MCP `session_state` 管理工具；当前实现是默认关闭、显式开启，避免普通 MCP server 默认暴露写入控制面。
2. 按真实权限模型决定是否默认启用 MCP `tool_transform` 管理工具；当前实现是默认关闭、显式开启，避免普通 MCP server 默认暴露工具定义改写控制面。
3. 继续收敛外部 CLI `api_flow`/`cli_real_flow` 这类被 ignore 的端到端测试；CLI 本身不是当前业务重点，但这些 flow 可作为 transport 和 fixture 迁移后的补充回归。

## 当前 A/B 清单

### A: 已确认要做且方向清晰

- session 是 Rust core 的业务能力，Python 只是 SDK facade，不再作为事实来源。
- session 跨进程共享只共享可序列化业务状态：metadata、binding、visibility、status、event。
- 连接池、MCP client handle、transport runtime 不共享，每个进程按 session state 自行 hydrate。
- memory backend 不承诺跨 store 实例或跨进程共享；真实共享使用 Redis/openkeyv Redis。
- session 写入使用 version/CAS，不接受临时 last-write-wins。
- Python `RustSession` 已接 Rust core，`extend_session()`、bind/unbind、list/use tool 已走 session-aware Rust 方法。
- Rust agent 使用 `SessionBuilder` / `SessionContext` 作为主入口，底层复用 Rust core session domain。
- session-scoped state/cache 已采用 Rust core `session_state`，Python `clear_cache()` 清理当前 session 的正式 state。
- Redis E2E 已通过临时 Redis 环境验证双 store 实例读写、真实 Redis 跨进程 Python/Rust 双向读写、switch 后跨进程读取、多进程并发 bind/extend 收敛，以及 `session_state` 的 Redis-only 用例。
- 旧 Python `find_user_session()` / `register_session_globally()` 已收敛为 Rust core metadata 能力：Python 不再用本地列表扫描或把 global id 写进 session_state，Rust/PyO3/Python facade 都能复用同一语义。
- Python 公共导出兼容已补齐到 session/context/resources_prompts/store_proxy/agent_proxy/operations mixin 层：旧 `mcpstore.core.context` 入口与顶层 `Session` / `SessionContext` / `MCPStoreContext` 都是 Rust-backed alias，旧对象式 proxy 和 operations mixin 入口委托 Rust-backed context，不引入 Python 私有 session、registry 或 service manager 逻辑。
- 旧 Python `list_changed_tools()` 已收敛为 Rust core 共享能力：Local/source-owner 进程按需刷新服务工具清单、比较 added/removed/updated、写回 registry/cache 并记录 service event；DB/read-only 进程只排队 `ServiceRefreshToolsRequested` 控制请求，不直接写共享 runtime state；Python `ResourcesPromptsMixin` 只做代理。

### B: 需要继续确认或选型

- API/MCP 输入形态：所有工具调用都显式传 `session_id/session_key`，还是允许 request/context 级默认业务 session。
- Python `switch_cache()` 语义：是否保留旧重建行为作为 fallback，还是统一强制走 Rust runtime migration。
- session export 默认保持关闭，只有 `include_sessions=True` 才导出运行中业务状态；session import 已采用“类型校验、引用校验、幂等允许、冲突不覆盖”的策略。
- 旧版 session 参数已采用明确映射：`default_timeout` 写入 Rust session `lease_seconds`；`session_prefix` 只在 Python facade 未显式传 `session_id` 时生成稳定 auto session id；`auto_cleanup` 作为共享 metadata policy flag，不在 Python 端伪造后台清理。
- session_state MCP 默认策略：Rust core/PyO3/Python facade/API/MCP opt-in 管理工具已完成；是否在具体部署中默认开启仍要按权限模型和业务入口决定。
- tool_transform MCP 默认策略：Rust core/PyO3/Python facade/API/MCP opt-in 管理工具已完成；是否在具体部署中默认开启仍要按权限模型和业务入口决定。
- OpenAPI executable virtual service：Rust core/PyO3/Python facade 已完成共享导入/分析 contract，但尚未实现 HTTP execution provider、auth、请求映射、响应映射、工具/resources/templates 运行时生命周期；这必须作为 Rust core/provider 能力推进，不能只在 Python facade 内伪兼容。
- 旧 tool transformation callable hooks：已明确不迁移为共享能力。它们是进程内 Python 回调，不能被 Rust agent/API/MCP server/跨进程后端一致使用；如果未来确实需要，应另行设计为可部署的策略服务或 WASM/DSL，不应混入当前 JSON registry。
- 旧 `list_changed_tools` 后台 monitor 策略：按需刷新+diff 已进入 Rust core；是否恢复旧版 notification debounce、定时 polling、reconnection 自动刷新，需要作为 Rust runtime 策略确认，不应回到 Python 私有 monitor。

## 已确认推进方案

采用 Rust core session + shared serializable state + CAS + Python facade wrapper。

这不是 Python 兼容层增强，而是把 session 升级为 Rust core 的业务能力。Python 后续只负责保留旧 SDK 体验；Rust agent、API、MCP server 都应能直接消费同一套 Rust session domain API。

### 默认采用的决策

以下决策作为当前实现默认值推进；如果后续业务需要调整，必须改计划和测试，而不是在代码里隐式分叉。

| 决策点 | 默认采用 | 原因 |
| --- | --- | --- |
| 默认 session scope | `store` scope | 最大程度兼容旧 Python store 级使用方式 |
| agent session | 支持，但显式指定 agent | Rust agent 需要一等能力，不能绕 Python |
| 未绑定 service | 继承当前 scope 可用服务 | 兼容旧行为，同时保留 bind 后的边界 |
| expired session | 不允许 `extend_session()` 复活 | 避免过期/关闭状态被普通续期语义混淆 |
| tool visibility | 第一版只做 allowlist | 保持语义清晰，避免 allow/deny/override 组合膨胀 |
| `clear_cache()` | 清理当前 session 的 `session_state` | 旧 Python 体验保留，但语义落在 Rust core 正式业务状态 |
| Redis CAS | 优先 Lua compare-and-put | 原子性明确，便于复用到 relation/status 更新 |
| API/MCP 接入 | API 与 MCP server 业务 session 已接 Rust domain；session_state MCP 管理工具默认关闭、显式开启 | API 控制面已可验证；MCP 管理能力与普通业务调用保持边界 |
| tool transformation | Rust core `tool_transforms` registry + Python thin wrapper + API/MCP opt-in 管理面 | Rust agent、Python agent、API、MCP server 消费同一套声明式规则；Python callable hook 不伪装成共享能力 |

### 当前执行批次

#### Batch 1: Rust cache/session 底座

状态：已完成。

目标：session 数据能作为 cache 一等集合进入 Rust core，并参与 snapshot/inspect/migration。

改动范围：

- `rust/crates/mcpstore/src/cache/models.rs`
- `rust/crates/mcpstore/src/cache/collections.rs`
- `rust/crates/mcpstore/src/cache/inspect.rs`
- `rust/crates/mcpstore/src/cache/layer_tests.rs`

验收：

- `sessions` entity 可写入和读取。
- `session_services`、`session_tools` relation 可进入 snapshot。
- `session_status` state 可进入 snapshot。
- `session_events` event 可进入 snapshot。
- `cache_inspect` 固定展示 session 集合计数。

#### Batch 2: Rust session domain API

状态：已完成。

目标：Rust core 能创建、读取、关闭、续期、绑定服务、限制工具可见性。

验收：Rust 单测覆盖 store scope、agent scope、同名 session 隔离、closed/expired 拒绝更新。

#### Batch 3: Session-aware resolver/call

状态：已完成。`list_tools_in_session`、`call_tool_in_session`、resources/prompts session-aware 路径已完成。

目标：Rust core 的 list_tools/call_tool/resources/prompts 支持 session 约束。

验收：绑定服务后只看到绑定服务工具；未绑定时继承 scope；session visibility 与 agent visibility 取交集。

#### Batch 4: PyO3/Python facade 对齐

状态：核心完成。Python 旧 session facade 已接到 Rust core，`_sessions` 只作为 wrapper cache；Python facade 与 PyO3 Rust core 已通过真实 Redis 跨进程共享 session；旧 `mcpstore.core.context` 与顶层 session/context 导出已恢复为 Rust-backed alias。

目标：Python 旧 session 体验不变，但真实状态来自 Rust core。

验收：`extend_session()` 不再抛 `NotImplementedError`；`_sessions` 只作为 wrapper cache；同一 Rust store 上的两个 Python wrapper 已能看到同一 session state；两个独立 Python/Rust 进程通过 Redis shared backend 能看到同一个 session。

#### Batch 5: Shared backend / Redis E2E / API-MCP 接入

状态：已完成当前验收目标。Redis Lua CAS 已在 Rust cache store 落地；真实 Redis 跨进程 Python/Rust 双向读写、switch 后读取、多进程并发 bind/extend 收敛已验证；API session 进程内真实 HTTP E2E 已补；MCP server 稳定 handler 集成测试已补；MCP server stdio/streamable-http transport E2E 已打开为默认测试并通过。

目标：证明跨进程 session 共享和热迁移成立，并开放给 API/MCP/Rust agent。

验收：Python 创建 Rust 读取、Rust 创建 Python 读取、Redis 并发 CAS、switch backend 后 session 仍在。

#### Batch 6: Session-scoped state/cache

状态：Rust core、PyO3、Python facade、API、MCP opt-in 管理工具已完成；Redis-only 测试已在临时 Redis 环境验证通过；具体部署是否默认启用 MCP 管理工具仍按权限模型决定。

目标：把旧 Python session cache 语义升级为 Rust core 可共享、可迁移、可审计的 session-scoped JSON state。

改动范围：

- `rust/crates/mcpstore/src/cache/models.rs`
- `rust/crates/mcpstore/src/session/`
- `rust/crates/mcpstore/src/cache/inspect.rs`
- `rust/apps/mcpstore/src/commands/api/mod.rs`
- `rust/apps/mcpstore/src/commands/mcp_server.rs`
- `rust/bindings/python/src/core_store.rs`
- `python/src/mcpstore/core/store/rust_backend.py`

验收：`session_state` 支持 set/get/list/delete/clear，显式 `_with_retry` 写入，snapshot/export/import/switch/inspect 迁移，Python `clear_cache()` 清当前 session state，两个 Python facade wrapper 共享同一 Rust core state，API 管理路由直接调用 Rust core 并拒绝 closed session 写入，MCP 管理工具默认关闭且显式开启后复用同一 Rust core state。

#### Batch 7: Rust-backed tool transformation

状态：Rust core、PyO3、Python facade、API、MCP opt-in 管理工具已完成；旧 Python callable hooks 明确不作为共享能力迁移。

目标：把旧 Python `tool_transformation.py` 中可跨进程、可被 Rust agent 复用的声明式能力升级为 Rust core `tool_transforms` registry。

改动范围：

- `rust/crates/mcpstore/src/cache/models.rs`
- `rust/crates/mcpstore/src/tool_transform.rs`
- `rust/crates/mcpstore/src/agent/`
- `rust/crates/mcpstore/src/service/`
- `rust/crates/mcpstore/src/session/`
- `rust/apps/mcpstore/src/commands/api/mod.rs`
- `rust/apps/mcpstore/src/commands/mcp_server.rs`
- `rust/bindings/python/src/core_store.rs`
- `python/src/mcpstore/core/store/rust_backend.py`
- `python/src/mcpstore/core/context/tool_transformation.py`

验收：声明式规则可写入 Rust core cache state；scoped/session/agent 工具列表使用 display name、description、参数 rename、hidden default、参数 description；工具调用可用 transformed name/arguments 解析回原始 service/tool/arguments；Python wrapper 只做 thin facade；API 和 MCP opt-in 管理工具直接调用 Rust core；MCP server 同一运行实例能动态读取最新 transform 规则；旧 callable hook 明确失败而不是伪兼容。

#### Batch 8: Python context resources/prompts 兼容入口

状态：已完成。Rust core/PyO3/API/Python context 已有 resources/prompts 业务能力；本批只恢复旧 Python `mcpstore.core.context.resources_prompts` 模块路径和 mixin 方法名，底层继续委托 Rust-backed context。

目标：旧 Python 用户能继续导入 `ResourcesPromptsMixin`，但不会把旧 orchestrator 或 Python 私有 monitor 带回核心路径。

改动范围：

- `python/src/mcpstore/core/context/resources_prompts.py`
- `python/src/mcpstore/core/context/__init__.py`
- `python/tests/test_pyo3_native_bridge.py`

验收：旧模块路径可导入；`list_resources`、`list_resource_templates`、`read_resource`、`list_prompts`、`get_prompt` 以及 async wrapper 都委托 Rust-backed context；`list_changed_tools` 委托 Rust-backed context，底层使用 Rust core 的按需工具刷新+diff 能力，不恢复旧 Python monitor。

#### Batch 9: Python context object proxy 兼容入口

状态：已完成。

目标：恢复旧 Python `mcpstore.core.context.store_proxy.StoreProxy` 与 `mcpstore.core.context.agent_proxy.AgentProxy` 模块路径和构造形态，同时保持 Rust core/RustStoreContext 为唯一业务行为来源。

改动范围：

- `python/src/mcpstore/core/context/store_proxy.py`
- `python/src/mcpstore/core/context/agent_proxy.py`
- `python/src/mcpstore/core/context/__init__.py`
- `python/tests/test_pyo3_native_bridge.py`

验收：旧模块路径可导入；`StoreProxy(context)` 和 `AgentProxy(context, agent_id)` 可构造；`get_id()`、`get_context()`、`find_agent()` 以及 resources/prompts/map_global/map_local 等行为委托 Rust-backed context；不恢复旧 Python registry/orchestrator 代理逻辑。

#### Batch 10: Python context operations mixin 兼容入口

状态：已完成。

目标：恢复旧 Python context operations 模块路径，让旧用户代码的 import 和可共享业务行为继续工作；所有真实业务行为仍委托 Rust-backed context。

改动范围：

- `python/src/mcpstore/core/context/service_operations.py`
- `python/src/mcpstore/core/context/tool_operations.py`
- `python/src/mcpstore/core/context/session_management.py`
- `python/src/mcpstore/core/context/agent_statistics.py`
- `python/src/mcpstore/core/context/advanced_features.py`
- `python/src/mcpstore/core/context/async_safe_service_management.py`
- `python/src/mcpstore/core/context/tool_proxy_annotations.py`
- `python/src/mcpstore/core/context/__init__.py`
- `python/tests/test_pyo3_native_bridge.py`

验收：旧模块路径可导入；service/tool/session/statistics mixin 方法委托 `RustStoreContext`；`AddServiceWaitStrategy` 和 `CallToolResultProtocol` 作为无状态兼容工具保留；旧 Python async-safe service manager factory 不恢复，避免把旧 registry/orchestrator 重新变成事实源。

#### Batch 11: user_session_id / global session 查询 Rust core 化

状态：已完成。

目标：恢复旧 Python `find_user_session()`、`create_shared_session()`、`register_session_globally()` 的业务语义，同时保证 Rust agent/PyO3/Python facade 使用同一个事实源。

改动范围：

- `rust/crates/mcpstore/src/session/mod.rs`
- `rust/bindings/python/src/core_store.rs`
- `python/src/mcpstore/core/store/rust_backend.py`
- `python/src/mcpstore/core/context/session_management.py`
- `rust/crates/mcpstore/src/session/tests.rs`
- `python/tests/test_pyo3_native_bridge.py`

验收：Rust core 支持按 `metadata.user_session_id` 全局查询 session，并支持 CAS 语义下更新 session metadata；PyO3 暴露同名能力；Python `find_user_session()` 与 `register_session_globally()` 只委托 Rust-backed context，不再本地扫描当前 scope，也不把 global id 写入 `session_state`。

#### Batch 12: list_changed_tools Rust core 化

状态：已完成按需刷新+diff 原子能力；旧 Python 后台 monitor 不恢复。

目标：恢复旧 Python `list_changed_tools()` 的可共享业务语义，同时保证 Rust agent/PyO3/Python facade 都消费同一套 Rust core 能力。

改动范围：

- `rust/crates/mcpstore/src/service/tool_changes.rs`
- `rust/crates/mcpstore/src/store/tool_changes.rs`
- `rust/crates/mcpstore/src/control/apply.rs`
- `rust/crates/mcpstore/src/control/queue.rs`
- `rust/crates/mcpstore/src/cache/service_projection.rs`
- `rust/bindings/python/src/core_store.rs`
- `python/src/mcpstore/core/store/rust_backend.py`
- `python/src/mcpstore/core/context/resources_prompts.py`
- `rust/crates/mcpstore/src/store/tests.rs`
- `python/tests/test_pyo3_native_bridge.py`

验收：Rust core 支持按需刷新服务工具并返回 added/removed/changed 快照；PyO3/Python context `list_changed_tools()` 只委托 Rust core，不恢复旧 Python 私有后台 monitor。

#### Batch 13: OpenAPI import/analyze Rust core 化

状态：已完成共享导入/分析 contract；可执行虚拟服务已在 Batch 14 完成。

目标：恢复旧 Python `import_api()` 的可共享分析能力，让 Rust agent/PyO3/Python facade 都能看到同一份 OpenAPI 导入结果；执行能力由后续 Batch 14 承接。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/lib.rs`
- `rust/crates/mcpstore/src/store/mod.rs`
- `rust/crates/mcpstore/src/cache/inspect.rs`
- `rust/crates/mcpstore/Cargo.toml`
- `rust/bindings/python/src/core_store.rs`
- `python/src/mcpstore/core/store/rust_backend.py`
- `python/src/mcpstore/core/context/advanced_features.py`
- `rust/crates/mcpstore/src/store/tests.rs`
- `python/tests/test_pyo3_native_bridge.py`

验收：Rust core 可从 URL 或内存 spec 导入 OpenAPI；分析结果持久化到 `state:openapi_imports` 并写入事件；默认 GET 映射 resource/resource_template，非 GET 映射 tool；PyO3/Python facade 可 import/get/list/last；API `/for_store/openapi_imports/*` 可 import/get/list；MCP server 显式开启后暴露 `mcpstore_openapi_import_*` 管理工具；Python 旧 `import_api()` 返回 context 并记录最后一次导入。

已验证：

- `cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `cd python && UV_CACHE_DIR=/Users/yuuu/.cache/uv-mcpstore CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target uv run --project . --with maturin maturin develop`
- `UV_CACHE_DIR=/Users/yuuu/.cache/uv-mcpstore uv run --project python --with pytest pytest python/tests/test_pyo3_native_bridge.py -q`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli --lib -- --nocapture`

#### Batch 14: OpenAPI executable virtual service Rust core 化

状态：已完成首个成熟可执行版本。

目标：OpenAPI endpoint 不只作为分析结果存在，而是作为 Rust core 的 `transport=openapi` 虚拟服务参与标准 service/tool/resource 生命周期。Python facade 不实现自己的 OpenAPI runtime；Rust agent、API、MCP server、PyO3/Python 都通过同一套 store 调用。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/service/connection.rs`
- `rust/crates/mcpstore/src/service/discovery.rs`
- `rust/crates/mcpstore/src/service/invocation.rs`
- `rust/crates/mcpstore/src/service/resources.rs`
- `rust/crates/mcpstore/src/cache/agent_projection.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `rust/apps/mcpstore/src/commands/api/mod.rs`
- `rust/apps/mcpstore/src/commands/mcp_server.rs`

验收：导入 OpenAPI spec 后 `runtime_executable=true`；Rust core 注册 `transport=openapi` service，并写入 `services`、`tools`、`service_tools`、`agent_services`、`service_status` 标准投影；`list_tools()` 能看到非 GET endpoint；`call_tool()` 可执行 OpenAPI POST/PUT/PATCH/DELETE 等 tool；`list_resources()`、`list_resource_templates()`、`read_resource()` 可执行 GET resource/template；DB/source reader 进程可通过标准投影刷新看到服务和工具，运行时 HTTP client 仍按进程本地创建。

当前支持边界：JSON/text HTTP 响应、path/query/header 参数、`body` JSON 请求体、service config 静态 headers、OpenAPI `servers[0].url` base URL。OpenAPI securitySchemes 到 import options/config 的映射已在 Batch 15 完成。暂不支持 OAuth flow/secret store 集成、multipart、streaming、二进制响应、复杂 content negotiation。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`

#### Batch 15: OpenAPI security/auth Rust core 化

状态：已完成首个标准认证版本。

目标：OpenAPI 可执行虚拟服务必须支持 spec 声明的安全认证要求，且认证配置作为 Rust core import options/store config 的一部分持久化和执行。Python facade、API、MCP server 不实现私有认证逻辑，只透传 `headers/auth` 到 Rust core；Rust agent 也可直接调用同一套 `OpenApiImportOptions`。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/service/invocation.rs`
- `rust/crates/mcpstore/src/service/resources.rs`
- `rust/crates/mcpstore/src/store/mod.rs`
- `rust/crates/mcpstore/src/lib.rs`
- `rust/crates/mcpstore/Cargo.toml`
- `rust/crates/mcpstore/src/store/tests.rs`
- `rust/apps/mcpstore/src/commands/api/mod.rs`
- `rust/apps/mcpstore/src/commands/mcp_server.rs`
- `rust/bindings/python/src/core_store.rs`
- `python/src/mcpstore/core/store/rust_backend.py`
- `python/src/mcpstore/core/context/advanced_features.py`
- `python/tests/test_pyo3_native_bridge.py`

验收：OpenAPI import result 保存 root `security` 与 `components.securitySchemes`；导入可传 `headers` 与 `auth`；runtime 按 operation security override/root security 解析要求；支持 `apiKey` header/query/cookie、HTTP bearer、HTTP basic；静态 headers 可满足 header apiKey/Authorization；tool 与 resource/resource_template 都使用同一套 runtime options；resource template URI 参数数量严格校验，缺失或多余参数直接报错；API/MCP/Python facade 都能传递 options。

当前支持边界：不实现 OAuth2/OpenID Connect flow 执行和 secret store 动态取密，遇到需要但无法由静态 token/header 满足的 OAuth/OpenID 要明确报错；仍不支持 multipart、streaming、二进制响应、复杂 content negotiation。OpenAPI HTTP 非 2xx 目前仍作为 runtime error 传播，是否转成 MCP tool `is_error=true` 需要单独设计。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore --lib -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli --lib -- --nocapture`
- `cd python && UV_CACHE_DIR=/Users/yuuu/.cache/uv-mcpstore CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target uv run --with maturin maturin develop`
- `UV_CACHE_DIR=/Users/yuuu/.cache/uv-mcpstore uv run --project python --with pytest pytest python/tests/test_pyo3_native_bridge.py -q`

#### Batch 16: OpenAPI runtime HTTP error 与常见 request body 支持

状态：已完成。

目标：让 OpenAPI executable virtual service 更接近真实 API 网关语义：目标 API 的业务 HTTP 错误不应被当成 mcpstore 服务故障；常见非 JSON request body 应由 Rust core 统一执行，而不是交给 Python facade 私自处理。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `rust/crates/mcpstore/Cargo.toml`
- `rust/Cargo.lock`

验收：OpenAPI tool 调用收到目标 API 非 2xx 响应时，返回 `ToolCallResult.is_error=true`，content 中包含 `status/reason/body`，但不把虚拟服务标记为 `ConnectionStatus::Error`，也不触发服务级 retry/circuit；resource 读取收到非 2xx 仍向调用方返回明确错误，因为 MCP resource read 没有 `is_error` 字段；request body 按 OpenAPI `requestBody.content` 支持 `application/json`、`application/x-www-form-urlencoded`、`multipart/form-data` 文本字段、`text/plain`；multipart 文件/二进制字段明确报错，不伪装成已支持。

当前支持边界：仍不实现 OAuth2/OpenID Connect flow 执行、secret store 动态取密、multipart 文件上传、二进制响应、streaming response、复杂 content negotiation、完整 OpenAPI parameter serialization/deep `$ref` expansion。这些需要单独设计数据模型和安全边界。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`

#### Batch 17: OpenAPI parameter serialization Rust core 化

状态：已完成常用成熟子集。

目标：OpenAPI executable virtual service 的参数序列化不能继续把 array/object 简单 JSON string 化；Rust core 必须按 OpenAPI 默认语义处理 path/query/header 参数，让 Rust agent、API/MCP server、PyO3/Python facade 使用同一套真实运行时行为。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：path/header 默认 `simple` style，array 逗号拼接、object 按 simple explode 语义拼接；query 默认 `form` style 且 `explode=true`，array 生成重复 query key，object explode 生成对象字段 query key；显式 `form explode=false` 逗号拼接；query `spaceDelimited` / `pipeDelimited` array 支持；不支持的 `deepObject`、path/header 非 simple style 明确报错。新增回归覆盖 path array、query form explode true/false、header array 的真实 HTTP 请求结果。

当前支持边界：仍不实现 OAuth2/OpenID Connect flow 执行、secret store 动态取密、multipart 文件上传、二进制响应、streaming response、复杂 content negotiation、`deepObject`、matrix/label path style、allowReserved、deep `$ref` expansion。这些需要单独设计数据模型和安全边界。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`

#### Batch 18: OpenAPI analyzer path parameters 与本地 `$ref` 支持

状态：已完成成熟基础版。

目标：真实 OpenAPI spec 常把公共 path 参数放在 path-item `parameters` 上，并通过 `#/components/...` 本地 `$ref` 复用 parameter、requestBody、schema、response。Rust core 的 import/analyze 必须先正确吸收这些结构，否则后续 runtime、Rust agent、API/MCP server、PyO3/Python facade 都会看到缺参数或错误 schema。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`

验收：analyzer 合并 path-item parameters 与 operation parameters；operation-level 同名同位置参数按 OpenAPI 规则覆盖 path-level 参数；支持当前 spec 内 JSON Pointer `$ref`，用于 parameter、requestBody、responses 及其嵌套 schema；无法解析的远程 `$ref`、非法本地 `$ref`、过深/循环 `$ref` 明确报错，不静默生成错误 schema。新增单元测试覆盖 path-level `$ref` parameter、operation 参数覆盖、`$ref` requestBody/schema 展开。

当前支持边界：仍不解析远程 `$ref` URL，不做跨文件 bundle，不处理循环 schema 的递归建模，不实现 OAuth2/OpenID Connect flow 执行、secret store 动态取密、multipart 文件上传、二进制响应、streaming response、复杂 content negotiation、`deepObject`、matrix/label path style、allowReserved。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 19: OpenAPI runtime required arguments validation

状态：已完成。

目标：OpenAPI executable virtual service 调用时，Rust core 应在本地校验 required path/query/header 参数和 required requestBody，避免把缺参请求发到远端后得到模糊 404/400，且让 Python facade、API/MCP server、Rust agent 都获得一致错误语义。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 在发 HTTP 请求前检查 required 参数；缺失 required path/query/header 参数时错误信息包含 `location.name`，例如 `path.sku`；缺失 required body 时错误信息包含 `body`；只把参数缺失或 `null` 判定为缺参，不把空字符串、空数组、空对象错误地视为缺失。新增回归覆盖缺 required path 参数和缺 required requestBody。

当前支持边界：这不是完整 JSON Schema validator，不校验类型、枚举、format、min/max、oneOf/allOf/anyOf；后续如果要做输入 schema validation，需要引入或实现 JSON Schema 校验策略，并明确是否对所有 MCP 工具统一生效。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`

#### Batch 20: OpenAPI response mimeType 保真

状态：已完成。

目标：OpenAPI executable virtual service 的 resource/resource_template 不能继续把所有响应都标记为 `application/json`。Rust core 应根据 OpenAPI `responses.content` 声明和目标 HTTP `Content-Type` 响应头保留实际媒体类型，让 Rust agent、API/MCP server、PyO3/Python facade 读取资源时获得一致的 `mimeType`。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：`list_resources()` / `list_resource_templates()` 从 OpenAPI response content 推导默认 `mimeType`；`read_resource()` 用实际 HTTP response `Content-Type` 填充 `contents[].mimeType`，并去掉 charset 等参数；JSON 响应仍按 JSON decode 后序列化为 text，`text/plain` 响应保持原始文本，不再伪装成 JSON。新增回归覆盖 `text/plain; charset=utf-8` 和 `application/json` 两类响应。

当前支持边界：这不是完整 content negotiation；runtime 还不会根据 Accept 在多个 response media type 中主动选择，也不支持二进制 response、streaming response、multipart response。二进制/streaming 需要先设计跨 Rust agent、API/MCP server、PyO3/Python 的数据结构和传输边界。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 21: OpenAPI runtime input schema validation

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 调用时，Rust core 应在发 HTTP 请求前执行输入 schema 的基础校验，避免类型、枚举、嵌套必填字段错误被远端 API 才发现；Python facade、API/MCP server、Rust agent 继续共用同一套 Rust runtime 语义。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 基于 analyzer 生成的 `component.input_schema` 校验传入参数；支持 object/string/number/integer/boolean/array、嵌套 object `required`、array `items`、`enum`、`nullable`；错误信息包含参数来源路径，例如 `query.limit`、`body.name`、`body.tags[1]`；未知额外字段暂不拒绝，避免破坏宽松 API 兼容。新增回归覆盖 query integer、query enum、body required、array item 类型，并验证合法请求仍可执行。

当前支持边界：这不是完整 JSON Schema/OpenAPI Schema validator；暂不处理 `format`、`minimum/maximum`、`minLength/maxLength`、`pattern`、`minItems/maxItems`、`additionalProperties`、`oneOf/allOf/anyOf/not`、discriminator、readOnly/writeOnly、schema composition 的复杂错误归因。后续如果要完整覆盖，应评估引入成熟 JSON Schema validator 或在 Rust core 明确建立 OpenAPI Schema 校验模块，而不是把逻辑散落到 Python facade。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 22: OpenAPI response Accept negotiation 基础版

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 在调用目标 API 时，应基于 OpenAPI `responses.content` 主动声明当前 runtime 能处理的响应媒体类型，避免在多个 content type 中优先拿到二进制或暂不支持的响应；Python facade、API/MCP server、Rust agent 共用 Rust runtime 的同一套 `Accept` 选择逻辑。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 在用户未显式配置 `Accept` header 时，从 `200/201/202/204/default` 及其它声明响应中收集当前支持的 `application/json`、`*+json`、`text/*` media type 并设置 `Accept`；用户通过 import options/static headers 显式设置的 `Accept` 保持优先；`list_resources()` / `list_resource_templates()` 的默认 `mimeType` 优先选择当前支持的响应类型，而不是误选 `image/png` 等暂不支持类型。新增回归覆盖同时声明 `image/png`、`application/json`、`text/plain` 时自动 `Accept` 只包含 JSON/text，且 resource 默认 `mimeType` 为 `application/json`。

当前支持边界：这不是完整 HTTP content negotiation；不实现 q-value 权重、不解析 server-driven variants、不支持二进制 response、streaming response、multipart response，也不把 unsupported response media type 自动转换为文件/bytes。二进制和 streaming 需要先设计跨 Rust agent、API/MCP server、PyO3/Python 的统一数据结构和传输生命周期。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 23: OpenAPI query allowReserved 支持

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 的 query 参数编码应支持 OpenAPI `allowReserved`，让 Rust core 能正确表达需要保留 RFC3986 reserved 字符的 API 查询参数；Python facade、API/MCP server、Rust agent 不实现各自的 URL 编码逻辑，统一走 Rust runtime。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 内部 query 参数记录编码策略；OpenAPI query 参数默认继续百分号编码 reserved 字符；`allowReserved=true` 时 query value 保留 RFC3986 reserved 字符；query auth/apiKey 仍按默认安全编码处理；新增回归覆盖同一值分别作为默认 query 和 `allowReserved=true` query 时生成不同 URL。

当前支持边界：`allowReserved` 只应用于 query 参数值，不改变 query 参数名编码，不应用于 path/header/cookie；仍不实现 `deepObject`、matrix/label path style、复杂 URL 模板、二进制/streaming response。包含 `#`、`&` 等 reserved 字符时，是否由远端按原始语义接受仍取决于 HTTP/URL 解析规则；runtime 只按 OpenAPI 要求控制编码策略，不额外发明 escaping 语义。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 24: OpenAPI path label/matrix style 支持

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 的 path 参数序列化应支持 OpenAPI `label` 与 `matrix` style，避免真实 API 中常见的路径模板只能退化为 `simple` 或直接报错；Python facade、API/MCP server、Rust agent 继续共用 Rust runtime 的路径编码逻辑。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：path 参数支持 `simple`、`label`、`matrix` style；array/object/scalar 按 OpenAPI style 生成分隔符，值本身继续 percent-encode；matrix array/object explode 生成重复 `;name=value` 或 `;key=value` 片段；新增真实 HTTP 回归覆盖 label array 与 matrix array explode 的路径请求。

当前支持边界：仍不实现 query `deepObject`、复杂 URL template、path 参数 `allowReserved` 扩展语义、二进制/streaming response；matrix/label 支持按当前 runtime 字符串替换路径模板实现，不引入独立 URI template 引擎。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 25: OpenAPI query deepObject style 支持

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 的 query 参数序列化应支持常见的 `deepObject` style，让对象型筛选参数可以由 Rust runtime 正确编码为 `filter[key]=value`；Python facade、API/MCP server、Rust agent 继续共用 Rust core 的参数编码能力，不在各入口重复实现。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：query 参数 `style=deepObject` 且 `explode=true` 时支持顶层 object，编码为 percent-encoded `name[key]=value` query pair；非 object、`explode=false`、数组/嵌套对象字段明确报错，不猜测非标准编码；新增真实 HTTP 回归覆盖 `filter[color]=red&filter[size]=large`。

当前支持边界：`deepObject` 只支持顶层 object 的 scalar/null 字段；不实现 nested object/array 的非标准扩展编码，不实现复杂 URI template、二进制/streaming response、multipart 文件字段或 secret store 动态取密。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 26: OpenAPI multipart binary file upload 契约

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 的 `multipart/form-data` 应支持 `type=string, format=binary` 文件字段，并且能力归属 Rust core。Python facade、API/MCP server、Rust agent 使用同一 JSON 文件参数契约，不在 Python 端实现本地专属上传逻辑。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：OpenAPI analyzer 将 binary 字段暴露为 `x_mcpstore_file=true` 的对象 schema，支持 `{ "bytes": "<base64>", "filename": "...", "mimeType": "..." }` 和 `{ "path": "...", "filename": "...", "mimeType": "..." }`；runtime 对文件对象做请求前校验，base64 入口适合跨进程/API/MCP，本地 path 入口由 Rust runtime 读取文件；真实 HTTP 回归覆盖 base64 bytes 和 local path 两种 multipart 文件上传。

当前支持边界：不支持 Python `bytes` 对象自动编码，调用方需传统一 JSON 契约；不支持 multipart 数组文件字段、远程 URI 文件引用、文件大小限制/streaming 上传、secret store 动态取密或二进制响应下载。path 入口读取的是 Rust runtime 进程可见的本地路径，跨机器/远程 API 应使用 base64 bytes 或后续 artifact/blob store。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 27: OpenAPI binary response 支持

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 不再把所有响应强制读取成 text；Rust core 应能按响应媒体类型和 OpenAPI binary schema 返回 MCP 兼容的 image/audio/resource blob。Python facade、API/MCP server、Rust agent 共用同一 Rust runtime 输出，不在 Python 端单独补二进制下载逻辑。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 内部响应体区分 JSON、Text、Bytes；JSON/text 成功响应保持原有行为；`image/*` 成功工具响应返回 `ContentItem::Image`，`audio/*` 返回 `ContentItem::Audio`，其它二进制响应返回 MCP resource blob；OpenAPI resource 读取二进制响应时返回 `contents[0].blob` 和 `mimeType`，不再把 bytes 塞进 `text`；Accept 协商把 runtime 已支持的二进制 media type 和 `type=string, format=binary` 响应 schema 纳入可接受范围。新增真实 HTTP 回归覆盖 image 工具响应和 PDF resource blob 响应。

当前支持边界：不实现 streaming response、大文件落盘/句柄式 artifact store、multipart response 解析、range request、自动文件名推断或 Python `bytes` 特化返回；二进制内容当前以 base64 放入 MCP 标准 image/audio/blob 结构，适合跨进程/API/MCP 传输但会带来内存和体积开销。大文件能力后续应进入统一 artifact/blob store，而不是让 Python facade 私自管理临时文件。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 28: OpenAPI HTTP(S) external `$ref` bundle

状态：已完成成熟基础版。

目标：真实 OpenAPI spec 常把公共 parameters/schemas/requestBodies/responses 拆到外部 JSON 文档。Rust core 的 import/analyze 必须在进入 analyzer 前完成 HTTP(S) 外部 `$ref` bundle，让 Python facade、API/MCP server、Rust agent 都看到同一份可执行的分析结果，而不是在 Python 端私自补解析。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：OpenAPI import 支持 HTTP(S) 绝对外部 `$ref` 和基于 `spec_url` 的相对外部 `$ref`；外部文档内的本地 `#/...` `$ref` 会按外部文档自身解析后嵌回主 spec；同一次 import 内远程 JSON 文档有缓存，重复引用不会重复抓取；检测到外部 `$ref` 循环时返回明确错误；新增真实 HTTP fixture 覆盖外部 path/query parameter、response schema、本地 nested `$ref`、resource_template 执行以及远程文档缓存。

当前支持边界：只支持 HTTP(S) JSON 外部文档；不支持 YAML 外部文档、文件系统 `$ref`、跨协议引用、完整 JSON Schema composition 建模、递归 schema 的保留式建模、远程缓存持久化或全量 bundle 输出 API。循环目前作为错误处理，不尝试生成递归 schema；这是为了让运行时工具 schema 和 MCP resource/template contract 保持可序列化、可验证。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 29: OpenAPI JSON/YAML 文本输入支持

状态：已完成成熟基础版。

目标：OpenAPI import 不应只接受 JSON object 或远程 JSON URL；真实 spec 常以 YAML 发布，且 API/MCP server/Python facade 都需要能提交内存中的 JSON/YAML 文本。解析能力归属 Rust core，Python facade 只做兼容分发，不引入 Python 私有 YAML 解析逻辑，保证 Rust agent、API/MCP server、Python 走同一条 import/analyze/runtime 路径。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `rust/bindings/python/src/core_store.rs`
- `rust/apps/mcpstore/src/commands/api/mod.rs`
- `rust/apps/mcpstore/src/commands/mcp_server.rs`
- `python/src/mcpstore/core/store/rust_backend.py`
- `python/tests/test_pyo3_native_bridge.py`
- `rust/Cargo.toml`
- `rust/crates/mcpstore/Cargo.toml`
- `rust/Cargo.lock`

验收：Rust core 新增 `parse_openapi_spec_text`，先按 JSON、再按 YAML 解析，并要求结果必须是 object；URL import 不依赖 content-type，统一读取 response text 后进入 Rust 文本解析；Rust core 暴露 `import_openapi_service_from_spec_text(_with_options)`；PyO3 暴露同名入口；Python facade 在 `import_openapi_service_from_spec()` 接收到 `str` 时转发给 Rust 文本入口；API/MCP server 新增 `spec_text` 参数，并与 `spec` 明确互斥。新增回归覆盖 URL YAML import、内存 YAML 文本 import，以及 Python facade 经 PyO3 调用 Rust YAML 解析。

当前支持边界：这批只解决 JSON/YAML spec 文本输入；外部 `$ref` 仍沿用 Batch 28 的 HTTP(S) JSON 文档能力，不扩展到外部 YAML 文档或文件系统 `$ref`；不提供完整 bundle 输出 API；不在 Python 端接受 PyYAML 依赖或 Python 私有解析路径；空白 `spec_text` 在 API/MCP 入口等同未提供，走 `spec_url` 拉取。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`

#### Batch 30: OpenAPI HTTP(S) external YAML `$ref` bundle

状态：已完成成熟基础版。

目标：Batch 28 已支持 HTTP(S) external `$ref`，Batch 29 已支持主 OpenAPI spec 的 YAML 输入；真实项目中外部 components 文档也常用 YAML。Rust core 的 `$ref` bundle 应复用同一 JSON/YAML 文本解析入口，避免主 spec 支持 YAML 但外部 `$ref` 仍只能 JSON 的断点。Python facade、API/MCP server、Rust agent 不需要各自新增逻辑。

改动范围：

- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：external `$ref` document fetch 从 JSON decoder 改为读取 response text，并复用 `parse_openapi_spec_text` 支持 JSON/YAML；外部文档缓存、相对 URL 解析、fragment pointer、本地 nested `$ref` 和循环检测继续沿用同一 resolver；新增真实 HTTP fixture 覆盖主 YAML spec 引用 `components.yaml#/...`，外部 YAML 文档内部再引用本地 `#/components/...`，import 后 resource template 可执行且远程 components 文档只请求一次。

当前支持边界：仍只支持 HTTP(S) external document；不支持文件系统 `$ref`、跨协议引用、持久化远程缓存、完整 bundle 输出 API、递归 schema 保留式建模或复杂 JSON Schema composition 语义。外部文档必须解析为 JSON/YAML object；数组/标量外部文档会明确报错。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`

#### Batch 31: OpenAPI multipart binary file array 支持

状态：已完成成熟基础版。

目标：Batch 26 已定义单文件 multipart JSON 契约，但真实 OpenAPI 常用 `type=array` + `items.format=binary` 表达多文件上传。Rust core runtime 应支持同一字段名发送多个 multipart file part，Python facade、API/MCP server、Rust agent 继续使用统一 JSON 文件对象数组契约，不在各入口单独拼 multipart。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：analyzer 会把 `array.items` 中的 binary string schema 暴露成 `items.x_mcpstore_file=true` 的文件对象数组；runtime 识别 multipart 原始 schema 中的 binary file array，并对数组内每个文件对象以同一个 field name 追加 multipart part；单文件字段仍走原有契约；输入校验能定位到 `body.files[0]` 这类数组元素错误路径。新增真实 HTTP 回归覆盖两个 base64 文件对象上传为同名 `files` part，并保留单文件 base64/path 回归。

当前支持边界：文件数组元素仍使用 Batch 26 的 `{ "bytes": "<base64>", "filename": "...", "mimeType": "..." }` 或 `{ "path": "..." }` 文件对象契约；不支持远程 URI 文件引用、文件大小限制/streaming 上传、目录上传、multipart nested object/array 的非标准编码或 artifact/blob store 句柄。path 入口仍读取 Rust runtime 进程可见的本地路径，跨进程/远程 API 调用应优先使用 base64 或后续 artifact store。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`

#### Batch 32: OpenAPI input schema 常用约束校验

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 的输入校验不只检查 required/type/enum，还应在 Rust runtime 层执行常见 JSON Schema/OpenAPI 约束，避免无效请求穿透到远端 HTTP 服务。Python facade、API/MCP server、Rust agent 共用同一套 Rust runtime 校验，不在 Python 端复制 schema validation。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `rust/crates/mcpstore/Cargo.toml`
- `rust/Cargo.lock`

验收：runtime 对 string 支持 `minLength`、`maxLength`、`pattern`；对 number/integer 支持 `minimum`、`maximum`，并兼容 OpenAPI 3.0 布尔型 `exclusiveMinimum/exclusiveMaximum` 和 JSON Schema/OpenAPI 3.1 数值型 `exclusiveMinimum/exclusiveMaximum`；对 array 支持 `minItems`、`maxItems`，且数组元素继续递归校验。新增真实工具调用回归覆盖请求前失败和成功请求，错误信息包含 query/body 嵌套路径。

当前支持边界：这批不是完整 JSON Schema validator；不支持 `oneOf/allOf/anyOf/not`、`additionalProperties`、`uniqueItems`、`multipleOf`、string `format` 语义或 discriminator。`pattern` 当前使用 Rust `regex` 引擎覆盖常见正则，和 JSON Schema 要求的 ECMAScript 正则方言不完全等价；如果后续需要严格兼容所有 OpenAPI pattern，应在 Rust core 引入 ECMA regex 兼容引擎或完整 JSON Schema validator，而不是 Python 端临时补。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 33: OpenAPI input schema 对象、数组、倍数约束校验

状态：已完成成熟基础版。

目标：在 Batch 32 的基础上继续补齐真实 API 常见输入约束，把 `additionalProperties`、`uniqueItems`、`multipleOf` 放到 Rust runtime 请求前校验里。Python facade、API、MCP server、Rust agent 继续共享同一套 Rust core 行为，不在各入口复制 schema validation。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：object schema 支持 `additionalProperties=false` 拒绝额外字段，支持 `additionalProperties` schema 校验额外字段；array schema 支持 `uniqueItems=true`；number/integer schema 支持 `multipleOf`，并拒绝无效的非正 `multipleOf` 声明。新增真实工具调用回归覆盖请求前失败和成功请求，错误信息包含 `body.metadata.extra`、`body.labels.color`、`body.tags`、`body.quantity` 等嵌套路径。

当前支持边界：这仍不是完整 JSON Schema validator；仍不支持 `oneOf/allOf/anyOf/not`、string `format` 语义、discriminator、readOnly/writeOnly、unevaluatedProperties 或 schema composition 下的复杂 `additionalProperties` 归因。`multipleOf` 当前按 runtime JSON number 转换后的数值语义校验，覆盖常见整数和小数约束；如需严格任意精度 JSON Schema 数学语义，应在 Rust core 引入成熟 JSON Schema validator 或专门 decimal number 模块，而不是 Python 端临时补。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_validate_input_schema_before_request -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 42: OpenAPI JSON 响应 schema 基础校验

状态：已完成成熟基础版。

目标：补齐 OpenAPI executable virtual service 的响应侧契约检查，让成功 JSON response 在返回给 tool/resource 调用方前按声明 response schema 做基础校验。Python facade、API、MCP server、Rust agent 继续共享 Rust core 的统一响应校验结果，不在 Python 端补私有 response validator。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：成功 JSON response 在 `writeOnly` 输出过滤后复用 Rust runtime 的基础 schema validator，能发现类型、required、对象/数组约束等明确契约违背；tool 调用和 resource 读取都会返回 `Invalid OpenAPI response for ...` 错误。新增真实 tool/resource 回归覆盖上游返回 `id` 类型错误、缺少 `username`、缺少嵌套 `metadata.public`、数组 `minItems` 违背。

当前支持边界：这一批不是完整 JSON Schema/OpenAPI response validator；只校验成功状态的 JSON 响应，不校验 HTTP 错误响应，不处理 text/binary 响应，不做类型转换、默认值填充、discriminator、unevaluatedProperties 或完整 evaluated-properties 归因。`writeOnly` schema 字段会先从响应视图移除，避免只写字段被过滤后反向触发 response schema required/type 错误。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_json_responses_validate_declared_schema -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_json_responses_filter_write_only_fields -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 43: OpenAPI 文件系统 external `$ref` bundle

状态：已完成成熟基础版。

目标：补齐 OpenAPI import 对本地文件 spec 和文件系统 external `$ref` 的共享解析能力。Rust core 在进入 analyzer 前统一 bundle 本地组件文件，让 Python facade、API、MCP server、Rust agent 都通过同一套导入路径获得可执行 OpenAPI virtual service，不在 Python 端补私有 YAML/file ref 解析。

改动范围：

- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `0626计划.md`

验收：`import_openapi_service()` 支持 `file://...` spec URL 和普通本地路径 spec URL；外部 `$ref` 支持相对本地文件、绝对本地文件和 `file://` URL；相对 `$ref` 按当前文档所在目录解析；外部本地 JSON/YAML 文档复用同一次 import 内存缓存；本地文档内的 `#/...` nested `$ref` 会按该文档自身解析后嵌回主 spec。新增真实本地文件 fixture 覆盖 path parameter、response schema、nested local `$ref`、resource_template 执行，以及 file URL 与普通路径两种导入入口。

当前支持边界：这一批只做本地文件读取和 import-time bundle，不提供独立 bundle 输出 API，不实现持久 remote/file ref cache，不做文件 sandbox/policy/allowlist，也不扩展到 zip/package/HTTP archive。普通本地路径按 Rust runtime 当前工作目录解析；跨进程/远程 API 调用如果要读取本地 path，必须确保运行 Rust core 的进程能访问该文件。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_import_bundles_external_file_refs -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_import_bundles_external_http_refs -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_import_bundles_external_yaml_http_refs -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `git diff --check`

#### Batch 44: OpenAPI 基础 bundle 输出 API

状态：已完成成熟基础版。

目标：把 import-time external `$ref` bundle 能力整理成可复用的 Rust core API，让 Rust agent 和 Python facade 都能直接获得 bundled OpenAPI spec，而不必注册 virtual service 或写入 `openapi_imports` state。Python 端只做薄包装，不实现私有 `$ref` 解析。

改动范围：

- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `rust/bindings/python/src/core_store.rs`
- `python/src/mcpstore/core/store/rust_backend.py`
- `python/tests/test_pyo3_native_bridge.py`
- `0626计划.md`

验收：Rust core 提供 `bundle_openapi_spec()`、`bundle_openapi_spec_from_text()`、`bundle_openapi_spec_from_value()`；PyO3/Python facade 提供 `bundle_openapi_spec()` 和 `bundle_openapi_spec_from_spec()`；返回值是 bundled spec `dict/Value`，不会注册 service，不会写入 `openapi_imports` state。新增 Rust 回归覆盖 HTTP external `$ref` bundle 输出、远程文档缓存和无 import state 副作用；新增 Python facade 回归覆盖本地文件 `$ref` 通过 Rust binding 完成 bundle。

当前支持边界：这是基础 bundle 输出 API，只返回 bundled OpenAPI JSON value；不输出依赖图、source map、诊断列表、bundle artifact metadata，不持久化 remote/file ref cache，也不把主文档内部所有 `#/...` local refs 强制 dereference 成平铺文档。API/MCP 管理入口已在 Batch 45 暴露；需要依赖元数据时应使用 Batch 46/47 的 bundle artifact 能力。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_bundle_spec_returns_external_refs_without_importing -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_import_bundles_external_file_refs -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target uv run --with maturin maturin develop`
- `cd python && uv run --with pytest pytest tests/test_pyo3_native_bridge.py -k openapi_file_refs_via_rust -q`
- `git diff --check`

#### Batch 45: OpenAPI bundle API/MCP 管理入口

状态：已完成成熟基础版。

目标：把 Batch 44 已落地的 Rust core bundle 输出能力正式暴露给 API 和 MCP 管理面，让远程 API 调用方与 Rust-side MCP agent 都能获取 bundled OpenAPI spec，而不需要通过 Python facade，也不需要执行 import、注册 virtual service 或写入 `openapi_imports` state。

改动范围：

- `rust/apps/mcpstore/src/commands/api/mod.rs`
- `rust/apps/mcpstore/src/commands/mcp_server.rs`
- `0626计划.md`

验收：API 增加 `POST /for_store/openapi_imports/bundle`，输入复用 `spec_url/spec/spec_text` 语义，返回 `data.bundle`；MCP server 在显式开启 OpenAPI 管理工具后增加 `mcpstore_openapi_bundle`，schema 只暴露 bundle 所需的 spec 输入，不暴露 import-only 的 `headers/auth`；两边都直接调用 Rust core `bundle_openapi_spec*` API。新增回归验证本地文件 external `$ref` 能被 bundle 展开，且调用后 `openapi_imports` 仍为空。

当前支持边界：这是裸 bundle 管理入口的薄封装，不改变 core bundle 能力边界；该入口仍只返回 bundled spec，不返回 source map、诊断列表、artifact metadata、持久 remote/file ref cache、服务端体积/权限策略或 OAuth/OpenID flow 执行。需要依赖图和文档列表时应使用 Batch 47 的 bundle artifact 管理入口。API bundle 入口要求 Rust API 进程能访问传入的本地文件路径；MCP bundle 工具仍受 `--expose-openapi-tools` 显式开关控制。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli store_route_bundles_openapi_without_importing -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli mcp_server_openapi_tools_are_explicit_and_use_rust_core -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli store_routes_manage_rust_core_openapi_imports -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `git diff --check`

#### Batch 46: OpenAPI 基础 bundle artifact 输出

状态：已完成成熟基础版。

目标：在裸 `bundle` 输出之外，给 Rust agent 和 Python facade 提供可审计的 OpenAPI bundle artifact：保留 bundled spec，同时返回参与 bundle 的文档列表、真实解析到的 external `$ref` 依赖边和诊断数组。Python 端只做 PyO3 薄包装，不实现私有 `$ref` 扫描或解析。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `rust/crates/mcpstore/src/lib.rs`
- `rust/crates/mcpstore/src/store/mod.rs`
- `rust/bindings/python/src/core_store.rs`
- `python/src/mcpstore/core/store/rust_backend.py`
- `python/tests/test_pyo3_native_bridge.py`
- `0626计划.md`

验收：Rust core 新增 `OpenApiBundleArtifact`、`OpenApiBundleDocument`、`OpenApiBundleDependency`、`OpenApiBundleDiagnostic` 公开模型，并从 crate facade 和 store module 导出；`MCPStore` 新增 `bundle_openapi_artifact()`、`bundle_openapi_artifact_from_text()`、`bundle_openapi_artifact_from_value()`；artifact 返回 `spec_url/bundle/documents/dependencies/diagnostics`，不会注册 service，也不会写入 `openapi_imports` state。PyO3/Python facade 新增 `bundle_openapi_artifact()` 和 `bundle_openapi_artifact_from_spec()`，直接返回 Rust core 序列化结果。新增 Rust/Python 回归验证 HTTP/file external `$ref` 的 dependency/document metadata，并验证无 import 副作用。

当前支持边界：这是基础 artifact，不是完整 source map。dependencies 只记录 resolver 实际处理的 external `$ref` 边；主文档内部 `#/...` local refs 不展开为 dependency graph；diagnostics 当前为结构化空数组预留，错误仍按现有 `Result` 失败返回，不做 warning 分级收集；不提供 per-node source location、source map、体积统计、hash 或持久 remote/file ref cache。API/MCP artifact 管理入口已在 Batch 47 暴露。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_bundle_artifact_reports_dependencies_without_importing -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_bundle_spec_returns_external_refs_without_importing -- --nocapture`
- `cd python && CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target uv run --with maturin maturin develop`
- `cd python && uv run --with pytest pytest tests/test_pyo3_native_bridge.py -k openapi_file_refs_via_rust -q`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `git diff --check`

#### Batch 47: OpenAPI bundle artifact API/MCP 管理入口

状态：已完成成熟基础版。

目标：把 Batch 46 的 Rust core bundle artifact 能力正式暴露给 API 和 MCP 管理面，让远程 API 调用方与 Rust-side MCP agent 都能拿到 bundled spec、参与解析的文档列表、external `$ref` 依赖边和 diagnostics 预留字段，同时仍然不执行 import、不注册 virtual service、不写入 `openapi_imports` state。

改动范围：

- `rust/apps/mcpstore/src/commands/api/mod.rs`
- `rust/apps/mcpstore/src/commands/mcp_server.rs`
- `0626计划.md`

验收：API 增加 `POST /for_store/openapi_imports/bundle_artifact`，输入复用 `spec_url/spec/spec_text` 语义，返回 `data.artifact`；MCP server 在显式开启 OpenAPI 管理工具后增加 `mcpstore_openapi_bundle_artifact`，schema 只暴露 bundle artifact 所需的 spec 输入，不暴露 import-only 的 `headers/auth`；两边都直接调用 Rust core `bundle_openapi_artifact*` API。新增回归验证本地文件 external `$ref` 能输出 `bundle/documents/dependencies/diagnostics`，且调用后 `openapi_imports` 仍为空。

当前支持边界：这是 bundle artifact 的管理入口薄封装，不扩大 Batch 46 的 artifact 语义；仍不是完整 source map，不提供 per-node source location、完整 warning 诊断收集、体积统计、hash、持久 remote/file ref cache、服务端体积/权限策略、secret store 或 OAuth/OpenID flow 执行。API artifact 入口要求 Rust API 进程能访问传入的本地文件路径；MCP artifact 工具仍受 `--expose-openapi-tools` 显式开关控制。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli store_route_bundles_openapi_artifact_without_importing -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli mcp_server_openapi_tools_are_explicit_and_use_rust_core -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli store_route_bundles_openapi_without_importing -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `git diff --check`

#### Batch 48: OpenAPI bundle artifact 文档身份元数据

状态：已完成成熟基础版。

目标：为后续持久 remote/file `$ref` cache 和更完整的 artifact 审计能力补齐稳定文档身份元数据。Rust core 在 bundle artifact 的 `documents` 中输出每个参与文档的内容摘要和字节长度；Python facade、API、MCP server、Rust agent 继续消费同一份 Rust core 序列化结果，不在 Python 或管理层重复计算。

改动范围：

- `rust/crates/mcpstore/Cargo.toml`
- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `python/tests/test_pyo3_native_bridge.py`
- `0626计划.md`

验收：`OpenApiBundleDocument` 新增 `content_hash` 与 `content_length`；root 文档来自文本输入时基于原始 JSON/YAML 文本计算 metadata，来自内存 `Value` 时基于稳定 JSON 序列化计算 metadata，external 文档基于实际读取/下载到的文档文本计算 metadata；`content_hash` 使用显式 `blake3:<hex>` 格式，`content_length` 为 UTF-8 字节长度。Rust core 和 Python facade 回归验证 artifact documents 都带 hash/size metadata，且 bundle/import 语义不变。

当前支持边界：这一批只建立 artifact document identity metadata，不实现持久 cache、不实现 ETag/Last-Modified 条件请求、不写入 cache backend、不做文档内容去重、不实现 source map 或 per-node source location。内存 `Value` 输入没有原始文本，metadata 使用 canonical JSON 表示，这是为了提供稳定身份而不是还原调用方原始格式。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_bundle_artifact_reports_dependencies_without_importing -- --nocapture`
- `cd python && CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target uv run --with maturin maturin develop`
- `cd python && uv run --with pytest pytest tests/test_pyo3_native_bridge.py -k openapi_file_refs_via_rust -q`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `git diff --check`

#### Batch 49: OpenAPI HTTP external `$ref` 共享文档缓存

状态：已完成成熟基础版。

目标：把 Batch 48 建立的文档身份元数据进一步落到 Rust core cache state，让 OpenAPI bundle/import/artifact 解析 HTTP(S) external `$ref` 时可以复用共享后端中的已解析文档。Python facade、API、MCP server、Rust agent 仍全部通过 Rust core 的 resolver 获得能力，不在各自入口实现私有缓存逻辑。

改动范围：

- `rust/crates/mcpstore/src/store/openapi.rs`
- `rust/crates/mcpstore/src/store/tests.rs`
- `0626计划.md`

验收：resolver 现在使用 `openapi_ref_documents` state record 缓存 HTTP(S) external `$ref` 文档；缓存 key 使用目标 URL 的 `blake3` 摘要，value 保存 `cache_version/url/source/fetched_at/expires_at/content_hash/content_length/document`；命中缓存时仍会填充 artifact documents 与 metadata，调用方行为与真实下载一致；普通 memory backend 测试证明同一 store 第二次 bundle 不再请求 external document；Redis 门控测试证明两个独立 store 实例在同一 namespace 下复用同一个 external document cache。导入、裸 bundle、bundle artifact 都经由同一套 Rust core resolver。

当前支持边界：这一批只缓存 HTTP(S) external `$ref` 文档，TTL 固定为 300 秒；不缓存 root spec，不缓存 file `$ref`，不做 ETag/Last-Modified 条件请求，不提供用户可配置 TTL/失效策略，不把大文档挪到 blob/artifact store。file `$ref` 暂时继续直接读取，避免在没有 mtime/长度校验的情况下产生陈旧本地缓存语义。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_bundle_writes_external_ref_document_cache -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore redis_backend_reuses_openapi_ref_document_cache_between_store_instances_when_available -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_bundle_artifact_reports_dependencies_without_importing -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli store_route_bundles_openapi_artifact_without_importing -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore-cli mcp_server_openapi_tools_are_explicit_and_use_rust_core -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
- `git diff --check`

#### Batch 40: OpenAPI JSON 响应 writeOnly 输出过滤

状态：已完成成熟基础版。

目标：补齐 OpenAPI executable virtual service 的响应侧基础安全语义，让 JSON response schema 中 `writeOnly: true` 的字段不会返回给 tool/resource 调用方。Python facade、API、MCP server、Rust agent 继续共享 Rust core 的统一响应视图，不在 Python 端补私有过滤。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：JSON tool response 和 resource response 在包装为 MCP 内容前，按声明 response schema 递归过滤 `writeOnly` 字段；保留 `readOnly` 字段；支持 properties、items、additionalProperties、allOf，以及当前值匹配的 anyOf/oneOf 分支。新增真实 tool/resource 回归覆盖 password/token/metadata.secret 被过滤。

当前支持边界：这一批不是完整 response schema validator；不校验响应是否符合 schema，不做类型转换，不处理非 JSON/binary/text 响应过滤，不实现 discriminator、unevaluatedProperties 或完整 evaluated-properties 归因。数组 item schema 本身为 `writeOnly` 时会删除对应元素，避免泄露只写值。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_json_responses_filter_write_only_fields -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 34: OpenAPI string format 基础校验

状态：已完成成熟基础版。

目标：OpenAPI executable virtual service 在请求前校验常见、可用成熟解析器明确判断的 string `format`，继续把输入契约收敛到 Rust runtime。Python facade、API、MCP server、Rust agent 共享同一套校验结果，不在 Python 端补私有格式检查。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 支持 `format=date` 的 `YYYY-MM-DD` 校验、`format=date-time` 的 RFC3339 校验、`format=uuid` 校验、`format=uri` 的 absolute URI 基础校验、`format=url` 的 HTTP(S) URL 校验；未知 format 保持忽略。新增真实工具调用回归覆盖 query UUID、body date/date-time/url/uri 的请求前失败和成功请求。

当前支持边界：这不是完整 JSON Schema format validator；不校验 `email`、`hostname`、`ipv4/ipv6`、`uri-reference`、`iri/iri-reference`、`json-pointer`、`regex` 等格式，避免用不完整正则制造假兼容。`uri` 当前使用 Rust `http::Uri` 做基础 absolute URI 校验，覆盖常见带 scheme 的 URI，但不承诺完整 RFC3986/IRI/URN 语义；如需严格格式全集，应在 Rust core 引入成熟 JSON Schema validator 或专门 URI/IRI parser。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_validate_input_schema_before_request -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 35: OpenAPI hostname/IP format 基础校验

状态：已完成成熟基础版。

目标：继续补齐 OpenAPI string `format` 中可稳定本地判断的网络标识符校验，把 hostname、IPv4、IPv6 的请求前错误收敛到 Rust runtime。Python facade、API、MCP server、Rust agent 继续共享同一套校验结果。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 支持 `format=hostname` 的 ASCII DNS label 基础校验，支持 `format=ipv4` 和 `format=ipv6` 的标准库地址解析校验；新增真实工具调用回归覆盖 body hostname/ipv4/ipv6 请求前失败和成功请求。

当前支持边界：hostname 基础版只接受 ASCII DNS label 语义：总长不超过 253、label 不为空且不超过 63、label 不以 `-` 开始或结束、字符只允许字母数字和连字符；不实现 IDNA/punycode 自动转换、Unicode 域名、通配符域名、SRV record 名称或 RFC 全量 hostname 边界。IPv4/IPv6 采用 Rust 标准库解析，不做网络可达性、私网/保留地址策略或 DNS 解析。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_validate_input_schema_before_request -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 36: OpenAPI email/regex format 基础校验

状态：已完成成熟基础版。

目标：继续补齐 OpenAPI string `format` 中可稳定本地判断的语法级校验，把 `email` 和 `regex` 的请求前错误收敛到 Rust runtime。Python facade、API、MCP server、Rust agent 继续共享同一套校验结果，不在 Python 端补私有格式检查。

改动范围：

- `rust/crates/mcpstore/Cargo.toml`
- `rust/Cargo.lock`
- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 支持 `format=email` 的 email address 语法校验，支持 `format=regex` 的正则表达式可编译性校验；新增真实工具调用回归覆盖 query regex、body email 的请求前失败和成功请求。

当前支持边界：`email` 使用 Rust core 依赖 `email_address` 做 RFC 风格地址语法校验，不做 MX/DNS 查询、SMTP 可投递性、账号存在性或业务域名策略。`regex` 使用 Rust `regex` 引擎验证表达式可编译性，语义边界与 Rust regex 方言一致，不承诺 JSON Schema/OpenAPI 生态里可能预期的 ECMAScript regex 全量兼容。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_validate_input_schema_before_request -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 37: OpenAPI schema composition 基础校验

状态：已完成成熟基础版。

目标：继续补齐 OpenAPI executable virtual service 的请求前输入 schema 校验，让常见 `allOf`、`anyOf`、`oneOf`、`not` 组合约束在 Rust runtime 层生效。Python facade、API、MCP server、Rust agent 继续共享同一套校验结果，不在 Python 端补私有 schema composition 逻辑。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 在普通业务 schema 上递归执行基础组合约束；`allOf` 收集所有子 schema 的具体错误；`anyOf` 要求至少匹配一个子 schema；`oneOf` 要求只匹配一个子 schema；`not` 要求不能匹配子 schema。新增真实工具调用回归覆盖 body 字段组合约束的请求前失败和成功请求。OpenAPI multipart 文件字段仍由 `x_mcpstore_file` 文件契约校验器处理，避免把 mcpstore 为 `bytes/path` 契约生成的内部 `oneOf` 误判为普通业务组合 schema。

当前支持边界：这仍不是完整 JSON Schema/OpenAPI Schema validator；组合匹配复用当前 runtime 的基础校验能力，不实现 discriminator、unevaluatedProperties、readOnly/writeOnly 语义、复杂 composition 下的 additionalProperties 归因、完整 OpenAPI/JSON Schema dialect 差异或完整错误树。文件上传参数的内部 schema 是 mcpstore 运行时契约，统一走文件参数校验，不承诺暴露为通用 JSON Schema composition 行为。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_validate_input_schema_before_request -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_support_common_request_body_media_types -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 38: OpenAPI JSON Pointer format 基础校验

状态：已完成成熟基础版。

目标：继续补齐 OpenAPI executable virtual service 的请求前 string `format` 校验，把 `json-pointer` 和 `relative-json-pointer` 的语法级错误收敛到 Rust runtime。Python facade、API、MCP server、Rust agent 继续共享同一套校验结果，不在 Python 端补私有格式检查。

改动范围：

- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 支持 `format=json-pointer` 的 RFC6901 基础语法校验，接受空字符串或 `/` 开头的 token 序列，并校验 `~0`/`~1` 转义；支持 `format=relative-json-pointer` 的非负整数前缀、可选 `#` 或 JSON Pointer tail，并拒绝 leading-zero offset。新增真实工具调用回归覆盖请求前失败和成功请求。

当前支持边界：这仍不是完整 JSON Schema/OpenAPI Schema validator；只做字符串语法校验，不解析 pointer 指向的数据、不检查目标是否存在、不处理 URI fragment 中的 percent-encoding 形式。`uri-reference`、`iri`、`iri-reference` 仍未支持；这些需要先选择成熟 RFC3986/IRI parser，避免用 HTTP URL parser 伪装完整兼容。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_validate_input_schema_before_request -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 39: OpenAPI readOnly/writeOnly 请求侧语义

状态：已完成成熟基础版。

目标：继续补齐 OpenAPI executable virtual service 的请求输入语义，让 `readOnly` / `writeOnly` 在 Rust core 中以请求视图统一生效。Python facade、API、MCP server、Rust agent 继续共享同一套 tool input schema 和请求前校验结果，不在 Python 端补私有处理。

改动范围：

- `rust/crates/mcpstore/src/openapi.rs`
- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：导入 OpenAPI request body schema 时生成请求侧 input schema：隐藏 `readOnly: true` 字段，从 request `required` 中移除只读字段，并保留 `writeOnly: true` 字段供 agent/调用方填写；runtime 在真实工具调用前基于原始 requestBody schema 拒绝客户端提交只读字段，避免因 schema 隐藏或 `additionalProperties` 导致只读字段透传。新增单元测试覆盖导入后的 request schema，新增真实工具调用回归覆盖 `list_tools` schema 暴露、readOnly 请求前失败、writeOnly 成功请求。

当前支持边界：这一批只处理请求侧语义；不做响应 schema 校验、响应字段过滤、响应侧 `writeOnly` 隐藏，也不实现完整 JSON Schema/OpenAPI dialect 的 evaluated-properties 归因。`allOf` 会作为确定性叠加约束扫描；`anyOf` / `oneOf` 只扫描当前值匹配的分支，避免未匹配分支中的只读字段误杀合法请求。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore request_input_schema_hides_read_only_body_fields -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_honor_read_only_and_write_only_request_fields -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_validate_input_schema_before_request -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`

#### Batch 41: OpenAPI URI/IRI reference format 基础校验

状态：已完成成熟基础版。

目标：继续补齐 OpenAPI executable virtual service 的请求前 string `format` 语义，把 `uri-reference`、`iri`、`iri-reference` 的语法级错误收敛到 Rust runtime。Python facade、API、MCP server、Rust agent 继续共享同一套格式校验结果，不在 Python 端补私有格式检查。

改动范围：

- `rust/crates/mcpstore/Cargo.toml`
- `rust/Cargo.lock`
- `rust/crates/mcpstore/src/openapi_runtime.rs`
- `rust/crates/mcpstore/src/store/tests.rs`

验收：runtime 使用 `iri-string` 做 RFC3986/RFC3987 语法级校验；`uri-reference` 支持绝对或相对 URI reference；`iri` 要求绝对 IRI；`iri-reference` 支持绝对或相对 IRI reference。新增真实工具调用回归覆盖非法空格引用、相对 IRI 不可作为 `iri`、Unicode 绝对 IRI 和 Unicode 相对 IRI reference 正例。

当前支持边界：这一批不是完整 JSON Schema/OpenAPI Schema validator；只校验字符串语法，不解析引用目标、不做网络访问、不做 IRI 到 URI 的规范化转换、不检查 scheme 业务策略，也不改变请求参数序列化。`uri` format 仍保持既有 absolute URI 基础校验；本批只补之前明确缺失的 reference/IRI format。

已验证：

- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo fmt`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi_tools_validate_input_schema_before_request -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo test -p mcpstore openapi -- --nocapture`
- `CARGO_TARGET_DIR=/Users/yuuu/.cache/mcpstore-cargo-target cargo check`
